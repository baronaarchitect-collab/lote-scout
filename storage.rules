rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Cada usuario solo accede a sus propias fotos: users/{uid}/...
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
